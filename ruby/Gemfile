source "https://rubygems.org"

# Runtime gems — these get packed into the wasm module.
# Resolved (Ruby 4.0 native): mutant 0.16.3, parser 3.3.12.0, sorbet-runtime.
gem "js"                 # ruby.wasm browser bridge
gem "rspec"
gem "mutant"
gem "mutant-rspec"
gem "kramdown"
gem "kramdown-parser-gfm"

# Build-time only — provides the `rbwasm` CLI; excluded from the packed module.
group :build do
  gem "ruby_wasm"
end
